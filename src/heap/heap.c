#include "heap.h"
#include <stdbool.h>
#include <stddef.h>

////////////////////////////////////////////////////////////////////////////////

static size_t parent_index(size_t i)
{
        return (i - 1) / 2;
}

////////////////////////////////////////////////////////////////////////////////

static size_t left_index(size_t i)
{
        return 2 * i + 1;
}

////////////////////////////////////////////////////////////////////////////////

static size_t right_index(size_t i)
{
        return 2 * i + 2;
}

////////////////////////////////////////////////////////////////////////////////

static void heapify_up(struct heap *h, size_t i)
{
        if (i == 0)
                return;
}

////////////////////////////////////////////////////////////////////////////////

static void heapify_down(struct heap *h, size_t i)
{
        if (i >= h->size)
                return;
}

////////////////////////////////////////////////////////////////////////////////

void heap_push(struct heap *h, size_t v)
{
}

////////////////////////////////////////////////////////////////////////////////

size_t heap_pop(struct heap *h)
{
        return 0;
}

////////////////////////////////////////////////////////////////////////////////

size_t heap_top(struct heap *h)
{
        return 0;
}

////////////////////////////////////////////////////////////////////////////////
